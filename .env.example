# ClinicCore 2.0 — Environment Variables
# Copy this file to .env and fill in all values
# NEVER commit .env to version control

# ============================================================
# SUPABASE DATABASE (Prisma connects to Supabase PostgreSQL)
# Both URLs are in Supabase dashboard → Settings → Database → Connection string
# ============================================================
# Transaction pooler — used at runtime (Transaction mode, port 6543)
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"
# Direct connection — used by `prisma migrate` only (Direct, port 5432)
DIRECT_URL="postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres"

# ============================================================
# SUPABASE AUTH
# ============================================================
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_ANON_KEY="your-supabase-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key"
JWT_SECRET="your-supabase-jwt-secret"

# ============================================================
# SUPABASE STORAGE — File Storage (replaces AWS S3)
# ============================================================
# Bucket name — create this bucket in Supabase Storage dashboard
# Files are stored with per-tenant prefix: {tenantId}/{category}/{filename}
SUPABASE_STORAGE_BUCKET="cliniccore-files"

# ============================================================
# APPLICATION
# ============================================================
NODE_ENV="development"
PORT=3001
FRONTEND_URL="http://localhost:5173"
API_BASE_URL="http://localhost:3001"

# ============================================================
# SMS (for patient OTP registration)
# ============================================================
# e.g. Twilio or AWS SNS
SMS_PROVIDER="twilio"
TWILIO_ACCOUNT_SID="your-twilio-account-sid"
TWILIO_AUTH_TOKEN="your-twilio-auth-token"
TWILIO_PHONE_NUMBER="+1234567890"

# ============================================================
# MONITORING (Sentry — error tracking)
# Create a project at https://sentry.io and paste the DSN here.
# Leave empty to disable Sentry (errors still logged via pino).
# ============================================================
SENTRY_DSN=""

# Frontend Sentry DSN (can be the same project or a separate one)
# Set this as a Render/CI environment variable — it is inlined at build time.
VITE_SENTRY_DSN=""

# ============================================================
# EMAIL (SMTP — for onboarding and transactional emails)
# Works with any SMTP provider: Gmail, SendGrid, Mailgun, AWS SES, etc.
# Leave SMTP_HOST empty to disable email sending (emails logged to console instead)
# ============================================================
SMTP_HOST="smtp.example.com"
SMTP_PORT=587
SMTP_USER="your-smtp-username"
SMTP_PASS="your-smtp-password"
SMTP_FROM="noreply@clinically.app"

# ============================================================
# SLACK NOTIFICATIONS (Phase 22)
# ============================================================
# Developer channel — task activity, PRs, regressions, deploys, prod issues
SLACK_WEBHOOK_DEV="https://hooks.slack.com/services/YOUR/DEV/WEBHOOK"
# Product Owner channel — staging sign-off, prod live, release notes, bug escalations
SLACK_WEBHOOK_PO="https://hooks.slack.com/services/YOUR/PO/WEBHOOK"
# Handle to ping on regressions and production failures
SLACK_ONCALL="@your-oncall-handle"

# Slack Bot Token — required for READING channel responses (outbound webhooks above are write-only)
# Create a Slack app at https://api.slack.com/apps → OAuth & Permissions
# Scopes needed: channels:history, channels:read, groups:history (for private channels)
SLACK_BOT_TOKEN="xoxb-your-bot-token"
# Channel IDs (not names) — right-click a channel in Slack → Copy Link → ID is the last segment
SLACK_DEV_CHANNEL_ID="C0XXXXXXXXX"
SLACK_PO_CHANNEL_ID="C0YYYYYYYYY"
